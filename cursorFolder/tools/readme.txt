1) use anaconda cmd and activate venv... in local it is base
2) run celery -A tasks worker -l info -P eventlet
3) run redis server and client
4) use cursor and cd c://python
5) run command python ..drag the run file.
