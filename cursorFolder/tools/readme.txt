1) use anaconda powershell prompt ONLY and activate venv... in local it is base
2) run redis server and client
3) cd to the folder where tasks.py file is kept
4) run celery -A tasks worker -l info -P eventlet 
5) use cursor and cd c://python
6) run command python ..drag the run file.
